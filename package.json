{
  "name": "sse2neon",
  "version": "1.6.0",
  "repo": "goodcleanfun/sse2neon",
  "install": "make install",
  "scripts": {
    "install": "clib install -fc $($npm_config_dev && echo '--dev')"
  }
}
